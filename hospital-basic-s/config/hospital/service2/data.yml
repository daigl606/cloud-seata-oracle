server:
   port: 8092
spring:
  cloud:
    alibaba:
      seata:
        tx-service-group: my-service-group
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.cebbank.service2.entity